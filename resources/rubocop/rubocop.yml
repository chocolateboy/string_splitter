# project-specific customizations and overrides

inherit_from: default.yml

AllCops:
  TargetRubyVersion: 2.3

inherit_mode:
  merge:
    - Include
    - Exclude

AllCops:
  Include:
    - 'Rakefile'
  Exclude:
    - 'resources/**/*'

Style/Semicolon:
  Exclude:
    - 'test/**/*.rb'
